<div class="container">
  <div class="row">
    <div class="col-md-8 offset-md-1">
      <h2>プロフィールを編集</h2>
      <%= form_with model:@user, local:true do |f| %>
      <table class="table">
        <tr>
          <th>プロフィール画像</th>
          <td><%= f.attachment_field :profile_image, class: "form-control-file user_image" %></td>
        </tr>
        <tr>
          <th>名前</th>
          <td><%= f.text_field :name, class: "form-control name" %></td>
        </tr>
        <tr>
          <th>自己紹介</th>
          <td><%= f.text_field :introduction, class: "form-control introduction" %></td>
        </tr>
        <tr>
          <th>メールアドレス</th>
          <td><%= f.text_field :email, class: "form-control email" %></td>
        </tr>
        <tr>
          <td><%= f.submit "変更を保存", class: "btn btn-outline-success" %></td>
        </tr>
      </table>
      <% end %>
    </div>
  </div>
</div>

