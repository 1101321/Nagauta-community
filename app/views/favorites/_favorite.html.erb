  <% if music.favorited_by?(current_user) %>
    <%= link_to music.favorites.count, music_favorites_path(music), method: :delete, remote: true, class: "fas fa-heart heart_red text-danger" %>
  <% else %>
    <%= link_to music.favorites.count, music_favorites_path(music), method: :post, remote: true, class: "fas fa-heart heart_blue" %>
  <% end %>